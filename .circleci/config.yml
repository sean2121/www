version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/ruby:2.4-node
      
      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/mongo:3.4.4 

    working_directory: ~/sean2121.github.io


    steps:
      - checkout

      # Download and cache dependencies

      - run:
          name: Ruby dependencies
          command: bundle install

      - run:
          name: Build
          command: bundle exec jekyll build --verbose
      
      - run:
          name: Test
          command: bundle exec htmlproofer ./_site --check-html --disable-external

      - add_ssh_keys:
          fingerprints: f8:3d:0c:5f:da:a3:d9:a0:21:0d:e8:23:6d:da:29:50
     
      - run:
         name: deploy
         command: scp -r sean2121.github.io/_site/* circleci_deployment@150-95-141-52:/var/www/test.com/public_html/

